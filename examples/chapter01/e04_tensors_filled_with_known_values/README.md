# Filled & Structured Tensors

Burn provides convenient constructors for creating tensors without existing data.

This example demonstrates `zeros`, `ones`, `full`, `eye`, and `arange`.

> Shapes are passed as arrays (e.g. `[2, 3]`), not separate arguments.
