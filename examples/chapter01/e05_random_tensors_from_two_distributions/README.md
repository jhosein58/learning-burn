# Random Tensors

Create tensors with random values using different probability distributions.

This example demonstrates uniform (`Distribution::Default`) and normal (`Distribution::Normal`) sampling.
